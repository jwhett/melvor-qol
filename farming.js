const farming = {
    ALLOTMENTS: 0,
    HERBS: 1,
    TREES: 2
}

loadFarmingArea(0) // Pick an area to operate on
harvestAll()
gloopAll()

// for i in list of seeds
selectSeed(i, true)
plantSeed(true)
